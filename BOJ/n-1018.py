n, m = map(int, input().split())
lst = []
w_start = b_start = 0

for i in range(n):
    lst += [input()]
    
for x in range(n - 7):
    for y in range(m - 7):
        for dy in range(8):
            for dx in range(4):
                if lst[y + dy][x + dx] !=
                if lst[y + dy][x + dx + 1]
        